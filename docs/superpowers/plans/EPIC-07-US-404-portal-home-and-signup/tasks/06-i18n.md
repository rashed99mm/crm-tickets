# Task 06 — i18n keys for home + signup (en & ar)

**Story:** `US-410`, `US-401` · **Criteria:** `ASG-1`, `ASG-3`
**Status:** done; verified by the end-of-work test run

## Files

- Modify `frontend/projects/common/src/lib/i18n/translations.ts` — add `portal.home.*` and
  `portal.signup.*` keys, en **and** ar.

## Implementation sequence

1. Add keys: home tagline/description and the Sign-in / Create-an-account action labels; signup
   title/subtitle, all six field labels, submit label, the "already have an account? sign in" link,
   the conflict message, and the account-created cue.
2. Both locales populated (ADR 0007 — every response/key carries both languages; the no-hardcoded
   string / bilingual specs verify presence).

## Tests and evidence

Covered indirectly by the component specs (they assert the keys render) and by the
`no-hardcoded-strings` / bilingual specs in `common`. Run the common suite at the end.
